Here's a summary of the changes:1.GUI Framework: The application now uses javax.swing to create a window.2.Main Class as JFrame: The Main class extends JFrame and sets up the GUI components.3.Start Button: A "Start Capturing Links" button is added. When clicked, it begins monitoring the clipboard.4.Stop Button: A "Stop Capturing & Generate BAT" button is added. When clicked, it stops monitoring the clipboard and generates the run_youtube_dlp.bat file with all the captured links.5.Link Display Area: A JTextArea is included to display the links as they are captured from the clipboard.6.Clipboard Monitoring:•A separate Thread (ClipboardMonitor) is used to continuously check the system clipboard without freezing the GUI.•It checks for new string content on the clipboard every 500 milliseconds.•It performs a basic check to see if the clipboard content looks like a URL (starts with "http://" or "https://") before adding it to the list.7.BAT File Generation: The logic for writing the .bat file is encapsulated in the generateBatFile() method, which is called when the "Stop" button is pressed. It uses the commandPrefix and commandSuffix you defined.8.Error Handling: Basic error handling for file I/O and clipboard access is included, with messages displayed in the JTextArea and JOptionPane pop-ups.9.Thread Safety: AtomicBoolean is used to safely control the clipboard monitoring thread.The application will now open a window. You can click "Start Capturing Links", copy YouTube links to your clipboard, and they will appear in the text area. When you're done, click "Stop Capturing & Generate BAT" to create the .bat file.

Here's what's been added:1.runBatButton: A new JButton named runBatButton has been added.2.Layout: The new button is placed alongside the "Start" and "Stop" buttons.3.runBatFile() Method: This new method is responsible for executing the run_youtube_dlp.bat file.•It first checks if the .bat file exists.•It uses ProcessBuilder to execute the command cmd.exe /c start <path_to_bat_file>, which opens the .bat file in a new command prompt window.•It includes error handling and provides feedback to the user through JOptionPane and the linkDisplayArea.4.Button State Management:•The runBatButton is initially disabled.•It becomes enabled after the .bat file is successfully generated (when you click "Stop Capturing & Generate BAT").•It is disabled again when you click "Start Capturing Links".Now, after generating the .bat file, you can click the "Run BAT File" button to execute it directly from the application.

jak stworzyc aplikacje:

mvn clean compile assembly:single
java -jar target/yt_dlp_link_converter-1.0-SNAPSHOT-jar-with-dependencies.jar


do szybkiego kopiowania linkow
uzyj wtyczke firefox - copy links on hover-
najezdzam myszka na element + (Ctrl+c)



-----------------------------------
DODANIE PRZYISKOW DO VIDEO

I will modify the Main.java file to:1.Rename existing variables for clarity (e.g., startButton to startMp3Button, capturedLinks to capturedMp3Links).2.Add new variables for video-specific buttons, link lists, capture states, and command parts.3.Create a separate ClipboardMonitorVideo class (or adapt the existing one) to handle video link capturing independently.4.Update initComponents() to initialize the new video buttons and set their initial states.5.Modify layoutComponents() to arrange both sets of buttons, perhaps in separate panels for better organization.6.Add new action listeners for the video buttons.7.Create new methods (startCapturingVideo, stopCapturingAndGenerateVideoBat, generateVideoBatFile, runVideoBatFile) that mirror the MP3 functionality but use the video-specific parameters.This will result in two distinct sections in your GUI, one for MP3 downloads and one for video downloads.Here's the updated Main.java code:Main.java-91 +177I have updated the Main.java file to include the video download functionality.Here's a breakdown of the changes:1.Renamed Variables: All existing MP3-related components and variables have been renamed with an Mp3 suffix (e.g., startButton -> startMp3Button, capturedLinks -> capturedMp3Links).2.New Video Components:•startVideoButton, stopVideoButton, runVideoBatButton for video operations.•capturedVideoLinks, isCapturingVideo, clipboardMonitorVideoThread for managing video link capture.•commandPrefixVideo, commandSuffixVideo, outputFilePathVideo constants specific to video downloads.3.Layout Improvements:•The JFrame size has been increased to 800x600.•Separate JPanels (mp3Panel and videoPanel) have been created for each set of buttons, each with a TitledBorder for clarity.•These panels are stacked vertically using a BoxLayout within a controlPanel in the NORTH region of the main frame.4.Refactored Logic:•The ClipboardMonitor class has been made generic. It now accepts the target List<String>, AtomicBoolean flag, JTextArea, and a String type (e.g., "MP3", "Video") in its constructor. This allows both MP3 and Video capturing to use the same monitor logic.•The generateBatFile and runBatFile methods have also been made generic, accepting parameters for the specific links, command parts, output path, and type. This significantly reduces code duplication.5.Button State Management:•When MP3 capturing starts, all video-related buttons are disabled.•When Video capturing starts, all MP3-related buttons are disabled.•When a capturing process stops, the corresponding "Run BAT File" button is enabled, and the "Start" button for the other type of download is re-enabled.Now you have two distinct sets of controls: one for generating .bat files to download MP3s and another for downloading videos, each with its own start, stop, and run buttons.

--------------------------------------
